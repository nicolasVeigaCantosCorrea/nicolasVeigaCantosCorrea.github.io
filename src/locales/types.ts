export type Translations = {
  navbar: {
    home: string;
    projects: string;
    experience: string;
    contact: string;
    language: string;
  };
  notFound: {
    title: string;
    description: string;
    button: string;
  };
  home: {
    title: string;
    description: string;
  };
};
