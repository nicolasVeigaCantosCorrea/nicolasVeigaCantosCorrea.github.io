export type Translations = {
  navbar: {
    home: string;
    projects: string;
    experience: string;
    contact: string;
    language: string;
  };

  home: {
    title: string;
    description: string;
  };
};
